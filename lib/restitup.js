/**
 * @author Jafeth Garcia
 */
const { RequestManager } = require('./utils/request-manager');
const { EventEmitter } = require('events');

class Restitup extends EventEmitter {
  constructor(options) {
    
  }
}

module.exports = { Restitup };